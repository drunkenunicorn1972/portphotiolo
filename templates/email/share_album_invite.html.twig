
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 30px;
            border-radius: 0 0 5px 5px;
        }
        .button {
            display: inline-block;
            padding: 12px 24px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 20px 0;
        }
        .message-box {
            background-color: white;
            padding: 15px;
            border-left: 4px solid #007bff;
            margin: 15px 0;
        }
    </style>
</head>
<body>
<div class="header">
    <h1>📸 You're Invited!</h1>
</div>
<div class="content">
    <p><strong>{{ senderName }}</strong> has shared an album with you:</p>

    <h2>{{ album.name }}</h2>

    {% if album.description %}
    <p><em>{{ album.description }}</em></p>
    {% endif %}

    {% if message %}
    <div class="message-box">
        <strong>Personal message:</strong><br>
        {{ message|nl2br }}
    </div>
    {% endif %}

    <p>Click the button below to view the album:</p>

    <div style="text-align: center;">
        <a href="{{ accessUrl }}" class="button">View Album</a>
    </div>

    <p style="font-size: 0.9em; color: #666;">
        This invitation link is personal and will expire in 30 days.
        You can access the album anytime using this link.
    </p>

    <hr style="margin: 30px 0; border: none; border-top: 1px solid #ddd;">

    <p style="font-size: 0.8em; color: #999;">
        If the button doesn't work, copy and paste this link into your browser:<br>
        <a href="{{ accessUrl }}">{{ accessUrl }}</a>
    </p>
</div>
</body>
</html>
